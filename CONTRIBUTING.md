# Contributing to BanglaScript

Thank you for your interest in contributing to BanglaScript! We welcome contributions from everyone.

## 🇧🇩 Code of Conduct

Please be respectful and kind to others. We want to create a welcoming environment for Bengali developers and everyone else.

## 🛠 Development Setup

1.  **Fork and Clone**
    ```bash
    git clone https://github.com/BengalEmpire/BanglaScript.git
    cd BanglaScript
    ```

2.  **Install Dependencies**
    ```bash
    npm install
    ```

3.  **Run Tests**
    ```bash
    npm test
    ```

## 🧪 Testing

We use [Jest](https://jestjs.io/) for testing. Please ensure all tests pass before submitting a PR.

-   Run all tests: `npm test`
-   Run in watch mode: `npm run test:watch`

## 📝 Style Guide

-   Use **Prettier** for code formatting.
-   Use **ESLint** for linting.
-   Run `npm run lint` and `npm run format` before committing.

## 🚀 Pull Request Process

1.  Create a new branch for your feature or fix: `git checkout -b feature/amazing-feature`.
2.  Commit your changes with meaningful commit messages.
3.  Push to your fork: `git push origin feature/amazing-feature`.
4.  Open a Pull Request against the `main` branch.
5.  Describe your changes and why they are needed.

## 🐛 Reporting Bugs

If you find a bug, please open an issue with:
-   A description of the bug.
-   Example code that causes the issue.
-   Expected vs actual behavior.

---

**ধন্যবাদ!** (Thank you!)
