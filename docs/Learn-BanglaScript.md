# BanglaScript শেখার প্র্যাকটিস গাইড: নতুনদের জন্য ধাপে ধাপে

হ্যালো! যদি তুমি প্রোগ্রামিং শিখতে চাও কিন্তু ইংরেজি কোড দেখে ভয় পাও, তাহলে **BanglaScript** তোমার জন্যই। এটি একটি টুল যা তোমাকে বাংলায় JavaScript কোড লিখতে সাহায্য করে। BanglaScript-এ লেখা কোড অটোমেটিক JavaScript-এ কনভার্ট হয় এবং চলে। এই ডকুমেন্টে আমরা ধাপে ধাপে শিখব কীভাবে শুরু করতে হয়—ইনস্টলেশন থেকে শুরু করে প্র্যাকটিস প্রোগ্রাম চালানো পর্যন্ত।

এই গাইডটি নতুনদের জন্য তৈরি, তাই সবকিছু সহজ ভাষায় ব্যাখ্যা করা হয়েছে। চলো শুরু করি!

---

## 📋 প্রয়োজনীয় জিনিস (Prerequisites)
BanglaScript চালাতে তোমার কম্পিউটারে **Node.js** থাকতে হবে। Node.js হলো JavaScript চালানোর একটি টুল।
- **Node.js ইনস্টল করো:** ওয়েবসাইট থেকে ডাউনলোড করো: [nodejs.org](https://nodejs.org)। সাধারণত LTS ভার্সন ইনস্টল করো (যেমন: v20 বা তার উপরে)।
- ইনস্টলের পর চেক করো: কমান্ড প্রম্পট (Windows) বা টার্মিনাল (Mac/Linux) খুলে টাইপ করো `node -v`। যদি ভার্সন দেখায়, তাহলে ঠিক আছে। 

[Need Help?](https://bangla-script.vercel.app/installation)

---

## 🛠️ BanglaScript ইনস্টল করা (Installation)
BanglaScript একটি NPM প্যাকেজ। NPM হলো Node.js-এর প্যাকেজ ম্যানেজার (যা Node.js-এর সাথে অটোমেটিক ইনস্টল হয়)।
1. কমান্ড প্রম্পট বা টার্মিনাল খুলো।
2. গ্লোবালি ইনস্টল করো (যাতে সব জায়গায় ব্যবহার করতে পারো):  
   ```
   npm i -g banglascript
   ```
3. চেক করো: `bjs --version` টাইপ করো। যদি ভার্সন দেখায় (যেমন: 2.0.0), তাহলে সফল!

**সমস্যা হলে:** যদি "command not found" দেখায়, তাহলে তোমার PATH ভ্যারিয়েবল চেক করো বা কম্পিউটার রিস্টার্ট করো।

---

## 🏗️ একটি নতুন প্রোজেক্ট তৈরি করা (Project Setup)
BanglaScript-এ প্রোজেক্ট তৈরি করা খুব সহজ।
1. টার্মিনালে যাও যেখানে প্রোজেক্ট রাখতে চাও।
2. টাইপ করো:  
   ```
   bjs init my-first-project
   ```
   - এটি একটি ফোল্ডার তৈরি করবে (`my-first-project`) যার মধ্যে `src/main.bjs` নামে একটি উদাহরণ ফাইল থাকবে।
3. প্রোজেক্টে ঢোকো:  
   ```
   cd my-first-project
   ```
4. এখন তোমার প্রোজেক্ট রেডি! `src` ফোল্ডারে তোমার কোড ফাইল (.bjs এক্সটেনশন) রাখো।

**টিপ:** যদি নতুন প্রোজেক্ট না চাও, তাহলে যেকোনো ফোল্ডারে .bjs ফাইল তৈরি করে চালাতে পারো।

---

## 🔍 কোড টেস্ট, বিল্ড এবং রান করা (Test, Build & Run)
BanglaScript-এ কোড লেখার পর তুমি এটাকে টেস্ট করতে পারো। এখানে মূল কমান্ডগুলো:
- **রান করা (Run):** কোড ডিরেক্ট চালাও এবং আউটপুট দেখো।  
  ```
  bjs run src/main.bjs
  ```
  - এটি কোড বিল্ড করে Node.js-এ চালায়।
  
- **বিল্ড করা (Build):** কোডকে JavaScript-এ কনভার্ট করো।  
  ```
  bjs build src/main.bjs
  ```
  - আউটপুট `build/main.js` ফাইলে সেভ হয়। তারপর চালাতে পারো: `node build/main.js`।
  
- **ওয়াচ মোড (Watch):** কোড চেঞ্জ করলে অটো বিল্ড হয়।  
  ```
  bjs watch src/main.bjs
  ```
  - এটি ফাইল মনিটর করে, চেঞ্জ হলে রি-বিল্ড করে। Ctrl+C দিয়ে থামাও।
  
- **কীওয়ার্ড দেখা:** BanglaScript-এর সব বাংলা কীওয়ার্ড দেখো।  
  ```
  bjs keywords
  ```

**সমস্যা ডিবাগ করা:** যদি এরর হয় (যেমন: সিনট্যাক্স এরর), তাহলে এরর মেসেজ পড়ো। উদাহরণ: "Unmatched bracket" মানে ব্র্যাকেট ঠিক করো।

---

## 🧑‍💻 প্র্যাকটিস প্রোগ্রামসমূহ (Practice Programs)
এখন চলো প্র্যাকটিস করি! নীচের উদাহরণগুলো কপি করে একটি .bjs ফাইলে পেস্ট করো (যেমন: `src/practice1.bjs`)। তারপর `bjs run src/practice1.bjs` দিয়ে চালাও। প্রত্যেকটির ব্যাখ্যা এবং আউটপুট দেওয়া আছে। শুরুতে সহজ থেকে, পরে একটু কঠিন।

### 🩵 ১. হ্যালো বাংলা! (প্রথম প্রোগ্রাম)
এটি তোমার প্রথম প্রোগ্রাম—শুধু একটা মেসেজ প্রিন্ট করবে।

```bangla
লিখো("হ্যালো বাংলা বিশ্ব!");
```

**ব্যাখ্যা:** `লিখো` হলো `console.log`-এর বাংলা ভার্সন। এটি স্ক্রিনে টেক্সট দেখায়।  
**চালানোর ধাপ:** ফাইলে সেভ করে `bjs run` করো।  
**আউটপুট:**  
```
হ্যালো বাংলা বিশ্ব!
```

### 🧮 ২. দুইটি সংখ্যার যোগফল (ভেরিয়েবল শেখা)
ভেরিয়েবল (চলক) ব্যবহার করে দুটি সংখ্যা যোগ করো।

```bangla
সংখ্যা a = ১০;
সংখ্যা b = ২০;
সংখ্যা ফলাফল = a + b;
লিখো("যোগফল: " + ফলাফল);
```

**ব্যাখ্যা:** `সংখ্যা` হলো `let`-এর মতো, ভেরিয়েবল ডিক্লেয়ার করে। `+` দিয়ে যোগ এবং স্ট্রিং জোড়া হয়।  
**টিপ:** সংখ্যা চেঞ্জ করে টেস্ট করো।  
**আউটপুট:**  
```
যোগফল: 30
```

### 📊 ৩. যদি–নাহলে (Condition) (সিদ্ধান্ত নেওয়া)
কন্ডিশন চেক করে মেসেজ দেখাও।

```bangla
সংখ্যা বয়স = ১৮;
যদি (বয়স >= ১৮) {
  লিখো("তুমি প্রাপ্তবয়স্ক!");
} নাহলে {
  লিখো("তুমি অপ্রাপ্তবয়স্ক!");
}
```

**ব্যাখ্যা:** `যদি` হলো `if`, `নাহলে` হলো `else`। বয়স চেঞ্জ করে দেখো কী হয়।  
**আউটপুট:**  
```
তুমি প্রাপ্তবয়স্ক!
```

### 🔁 ৪. লুপ ব্যবহার (রিপিট করা)
একই কাজ বারবার করো।

```bangla
জন্য (সংখ্যা i = ০; i < ৫; i = i + ১) {
  লিখো("গণনা: " + i);
}
```

**ব্যাখ্যা:** `জন্য` হলো `for` লুপ। `i` একটা কাউন্টার। ৫-এর জায়গায় ১০ দিয়ে টেস্ট করো।  
**আউটপুট:**  
```
গণনা: 0
গণনা: 1
গণনা: 2
গণনা: 3
গণনা: 4
```

### 🧱 ৫. অনুষ্ঠান (Function) (কোড রিইউজ করা)
একটা ফাংশন তৈরি করো যা বর্গ গণনা করে।

```bangla
অনুষ্ঠান বর্গ(x) {
  প্রেরণ x * x;
}

লিখো(বর্গ(৫));
```

**ব্যাখ্যা:** `অনুষ্ঠান` হলো `function`, `প্রেরণ` হলো `return`। ফাংশন কল করে আউটপুট দেখো।  
**আউটপুট:**  
```
25
```

### 🧺 ৬. তালিকা (Array) (লিস্ট স্টোর করা)
ফলের লিস্ট তৈরি করো।

```bangla
সংখ্যা ফলমূল = ["আম", "কলা", "লিচু"];
লিখো("প্রথম ফল: " + ফলমূল[০]);
```

**ব্যাখ্যা:** অ্যারে হলো লিস্ট। ইন্ডেক্স ০ থেকে শুরু। নতুন ফল যোগ করো এবং টেস্ট করো।  
**আউটপুট:**  
```
প্রথম ফল: আম
```

### 🧭 ৭. সমাবেশ (Object) (ডাটা গ্রুপ করা)
একটা অবজেক্ট তৈরি করো।

```bangla
সংখ্যা ব্যক্তি = {
  নাম: "রাহিম",
  বয়স: ২২,
  শহর: "ঢাকা"
};
লিখো("নাম: " + ব্যক্তি.নাম);
লিখো("বয়স: " + ব্যক্তি.বয়স);
```

**ব্যাখ্যা:** অবজেক্ট হলো কী-ভ্যালু পেয়ার। `.` দিয়ে অ্যাক্সেস করো। তোমার নিজের ডাটা দিয়ে চেঞ্জ করো।  
**আউটপুট:**  
```
নাম: রাহিম
বয়স: 22
```

### 🎯 ৮. ফাংশন থেকে শর্ত সহ রিটার্ন
ফাংশনে কন্ডিশন যোগ করো।

```bangla
অনুষ্ঠান ধনাত্মক_নাকি(সংখ্যা) {
  যদি (সংখ্যা > ০) {
    প্রেরণ "ধনাত্মক";
  } নাহলে {
    প্রেরণ "ঋণাত্মক বা শূন্য";
  }
}

লিখো(ধনাত্মক_নাকি(৫));
লিখো(ধনাত্মক_নাকি(-৩));
```

**ব্যাখ্যা:** ফাংশনের মধ্যে if-else ব্যবহার। বিভিন্ন সংখ্যা দিয়ে টেস্ট করো।  
**আউটপুট:**  
```
ধনাত্মক
ঋণাত্মক বা শূন্য
```

### ⚙️ ৯. নেস্টেড লুপ (লুপের মধ্যে লুপ)
একাধিক লুপ ব্যবহার।

```bangla
জন্য (সংখ্যা i = ১; i <= ৩; i = i + ১) {
  জন্য (সংখ্যা j = ১; j <= ২; j = j + ১) {
    লিখো("i=" + i + ", j=" + j);
  }
}
```

**ব্যাখ্যা:** বাইরের লুপ i চালায়, ভিতরের j। এটি টেবিল বা গ্রিড তৈরিতে সাহায্য করে।  
**আউটপুট:**  
```
i=1, j=1
i=1, j=2
i=2, j=1
i=2, j=2
i=3, j=1
i=3, j=2
```

### 🧮 ১০. গাণিতিক অপারেশন টেস্ট
সব ম্যাথ অপারেটর টেস্ট করো।

```bangla
সংখ্যা x = ১০;
সংখ্যা y = ৩;
লিখো("যোগ: " + (x + y));
লিখো("বিয়োগ: " + (x - y));
লিখো("গুণ: " + (x * y));
লিখো("ভাগ: " + (x / y));
লিখো("ভাগশেষ: " + (x % y));
```

**ব্যাখ্যা:** + - * / % ব্যবহার। ভাগে দশমিক আসে।  
**আউটপুট:**  
```
যোগ: 13
বিয়োগ: 7
গুণ: 30
ভাগ: 3.3333333333333335
ভাগশেষ: 1
```

### 🪄 ১১. BanglaScript Mini Project – ক্যালকুলেটর 🎯
একটা সিম্পল ক্যালকুলেটর তৈরি করো।

```bangla
অনুষ্ঠান ক্যালকুলেটর(a, b, অপারেশন) {
  যদি (অপারেশন == "+") {
    প্রেরণ a + b;
  } নাহলে যদি (অপারেশন == "-") {
    প্রেরণ a - b;
  } নাহলে যদি (অপারেশন == "*") {
    প্রেরণ a * b;
  } নাহলে যদি (অপারেশন == "/") {
    প্রেরণ a / b;
  } নাহলে {
    প্রেরণ "অজানা অপারেশন";
  }
}

লিখো(ক্যালকুলেটর(১০, ৫, "+"));
লিখো(ক্যালকুলেটর(১০, ৫, "*"));
```

**ব্যাখ্যা:** ফাংশন + if-else চেইন। নতুন অপারেশন যোগ করে এক্সপ্যান্ড করো।  
**আউটপুট:**  
```
15
50
```

---

## 📘 সমাপ্তি এবং পরবর্তী ধাপ
এই প্র্যাকটিসগুলো শেষ করলে তুমি BanglaScript-এর বেসিক (ভেরিয়েবল, লুপ, ফাংশন, অবজেক্ট) আয়ত্তে আনবে। এখন তোমার নিজের প্রোজেক্ট তৈরি করো—যেমন: একটা গেম বা টুডু লিস্ট।

**অতিরিক্ত টিপস:**
- **এরর হলে:** কোডের লাইন নম্বর চেক করো। ব্র্যাকেট ({}) ম্যাচ করে কিনা দেখো।
- **আরও শেখা:** `bjs keywords` দিয়ে সব কীওয়ার্ড দেখো। JavaScript-এর বেসিক টিউটোরিয়াল পড়ো (বাংলায় সার্চ করো)।
- **হেল্প:** যদি সমস্যা হয়, GitHub-এ BanglaScript রিপোজিটরি সার্চ করো বা ফোরামে জিজ্ঞাসা করো।
- **আপডেট:** নতুন ভার্সন চেক করো: `npm update -g banglascript`।

**BanglaScript — বাংলায় কোড লেখো, JavaScript চলে! 💡**  

Web: [Learn More](https://bangla-script.vercel.app/learn-banglascript)