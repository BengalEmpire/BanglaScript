# BanglaScript (বাংলাস্ক্রিপ্ট)

**BanglaScript** একটি পরীক্ষামূলক (experimental) প্রোগ্রামিং ভাষা, যা বাংলা ভাষায় কোড লেখার সুযোগ দেয়।
এই ভাষা আসলে **JavaScript-এর উপরে ভিত্তি করে তৈরি**, অর্থাৎ তোমার বাংলা কোড শেষ পর্যন্ত JavaScript-এ রূপান্তরিত হয় এবং Node.js বা ওয়েব ব্রাউজারে চলে।

---

## 🚀 শুরু করি — তোমার প্রথম বাংলা প্রোগ্রাম

একটি ফাইল তৈরি করো `main.bjs` নামে:

```bangla
অনুষ্ঠান মূল() {
  লিখো("হ্যালো বাংলা বিশ্ব!");
  প্রেরণ ০;
}

মূল();
```

তারপর কম্পাইল করো:

```bash
banglascript main.bjs --run
```

অথবা সরাসরি Node ব্যবহার করলে:

```bash
node banglascript.js main.bjs --run
```

আউটপুট হবে:

```
হ্যালো বাংলা বিশ্ব!
```

---

## 🧩 ১. ভ্যারিয়েবল বা পরিবর্তনশীল

তুমি সংখ্যা, বাক্য (string), বা তালিকা রাখতে পারো।

```bangla
সংখ্যা x = ১০;
সংখ্যা নাম = "ফারহান";
লিখো(x);
লিখো(নাম);
```

> 🔍 "সংখ্যা" = let / var, অর্থাৎ ভ্যারিয়েবল ঘোষণা করতে ব্যবহৃত হয়।

---

## 🧮 ২. গাণিতিক অপারেশন

তুমি +, -, *, /, % ব্যবহার করতে পারো:

```bangla
সংখ্যা a = ৫;
সংখ্যা b = ৩;
লিখো(a + b); // ৮
লিখো(a * b); // ১৫
```

---

## 🧠 ৩. শর্ত (Condition)

`যদি` এবং `নাহলে` দিয়ে শর্ত লেখা যায়:

```bangla
সংখ্যা বয়স = ১৮;

যদি (বয়স >= ১৮) {
  লিখো("তুমি প্রাপ্তবয়স্ক!");
} নাহলে {
  লিখো("তুমি এখনো অপ্রাপ্তবয়স্ক!");
}
```

---

## 🔁 ৪. লুপ (Loop)

### 📍 `জন্য` (for loop)

```bangla
জন্য (সংখ্যা i = ০; i < ৫; i = i + ১) {
  লিখো("গণনা: " + i);
}
```

### 📍 `যখন` (while loop)

```bangla
সংখ্যা i = ০;
যখন (i < ৩) {
  লিখো("i = " + i);
  i = i + ১;
}
```

---

## 🧱 ৫. ফাংশন বা অনুষ্ঠান

বাংলায় function লেখা যায় `অনুষ্ঠান` keyword দিয়ে:

```bangla
অনুষ্ঠান যোগফল(a, b) {
  প্রেরণ a + b;
}

লিখো(যোগফল(৫, ৩)); // ৮
```

> 🔹 "প্রেরণ" = return

---

## 🧺 ৬. তালিকা (Array)

```bangla
সংখ্যা ফলমূল = ["আম", "কলা", "লিচু"];
লিখো(ফলমূল[০]); // আম
```

---

## 🧭 ৭. সমাবেশ (Object)

```bangla
সংখ্যা ব্যক্তি = {
  নাম: "রফিক",
  বয়স: ২৫,
  শহর: "ঢাকা"
};

লিখো(ব্যক্তি.নাম);
```

---

## 🧮 ৮. লজিক্যাল অপারেটর

```bangla
যদি (x > ৫ && y < ১০) {
  লিখো("শর্ত মিলেছে!");
}
```

---

## 🗣️ ৯. মন্তব্য (Comment)

```bangla
// এটি একটি এক লাইনের মন্তব্য
/* এটি
   বহু লাইনের মন্তব্য */
```

---

## 🔢 ১০. ডেটা টাইপ

| বাংলা শব্দ | অর্থ (JavaScript Type) | উদাহরণ          |
| ---------- | ---------------------- | --------------- |
| সংখ্যা     | Number                 | ১০, ৩.১৪        |
| বাক্য      | String                 | "হ্যালো"        |
| সত্য       | true                   | সত্য            |
| মিথ্যা     | false                  | মিথ্যা          |
| শূন্য      | null                   | শূন্য           |
| তালিকা     | Array                  | ["আম", "কলা"]   |
| সমাবেশ     | Object                 | { নাম: "রফিক" } |

---

## 🧾 ১১. Keyword Vocabulary (বাংলা → JavaScript Mapping Table)

| বাংলা কীওয়ার্ড | রূপান্তরিত JavaScript কোড | বর্ণনা                          |
| -------------- | ------------------------- | ------------------------------- |
| সংখ্যা         | let                       | ভ্যারিয়েবল ঘোষণা                |
| বাক্য          | let                       | স্ট্রিং রাখার জন্য              |
| অনুষ্ঠান       | function                  | ফাংশন                           |
| প্রেরণ         | return                    | মান ফেরত দেয়া                   |
| লিখো           | console.log               | আউটপুট প্রদর্শন                 |
| সমস্যা_লিখো    | console.error             | ত্রুটি প্রদর্শন                 |
| যদি            | if                        | শর্ত যাচাই                      |
| নাহলে          | else                      | বিকল্প শর্ত                     |
| যখন            | while                     | লুপ                             |
| জন্য           | for                       | লুপ                             |
| সত্য           | true                      | বুলিয়ান সত্য মান                |
| মিথ্যা         | false                     | বুলিয়ান মিথ্যা মান              |
| শূন্য          | null                      | শূন্য মান                       |
| বন্ধ_করো       | break                     | লুপ থামানো                      |
| চালিয়ে_যাও     | continue                  | পরবর্তী iteration এ যাও         |
| নতুন           | new                       | অবজেক্ট তৈরি                    |
| শ্রেণি         | class                     | ক্লাস                           |
| গঠন            | constructor               | কনস্ট্রাক্টর ফাংশন              |
| আমদানি         | import                    | মডিউল আনা                       |
| রপ্তানি        | export                    | মডিউল পাঠানো                    |
| চেষ্টা         | try                       | ত্রুটি ধরার ব্লক শুরু           |
| ধরো            | catch                     | ত্রুটি ধরা                      |
| অবশেষে         | finally                   | চূড়ান্ত ব্লক                    |
| ফেলা           | throw                     | ত্রুটি নিক্ষেপ                  |
| অপেক্ষা        | await                     | async function এর মধ্যে অপেক্ষা |
| প্রতিজ্ঞা      | Promise                   | asynchronous operation          |
| শুরু_করো       | run/start                 | CLI command (future use)        |

> 🔧 ভবিষ্যতে এই তালিকা আরও বাড়বে (যেমন: switch, case, default, async, arrow function ইত্যাদি)।

---

## ⚙️ ১২. CLI কমান্ড পরিকল্পনা (ভবিষ্যৎ)

| কমান্ড                        | কাজ                                      |
| ----------------------------- | ---------------------------------------- |
| `banglascript main.bjs`       | `.bjs` কোডকে `.js` এ রূপান্তর করবে       |
| `banglascript main.bjs --run` | `.bjs` কোড transpile করে সরাসরি রান করবে |
| `banglascript build`          | পুরো প্রকল্প compile করবে                |
| `banglascript watch`          | ফাইল পরিবর্তন হলে auto-compile করবে      |

---

## 🌱 ভবিষ্যৎ পরিকল্পনা

* বাংলা ত্রুটি বার্তা (logs)
* একটি Online (WEB ভিত্তিক) playground, যেখানে BanglaScript লিখা যাবে এবং আউটপুট দেখা যাবে। সেখানে BanglaScript কে JavaScript এ convert করা যাবে।
* VS Code syntax highlighting ও `.bjs` extension
* BanglaScript CLI (Command Line Interface)
* Node.js ও Browser runtime support
* Input system (`পড়ো()` ফাংশন)
* বাংলা লাইব্রেরি সমর্থন
* Linguist configuration for highlighting .bjs extension
* এটিকে সহজতর করা হবে। যার মাধ্যমে সময়ে Code Editor (VSCode), এ BanglaScript কোড লিখা যাবে। 

---

**BanglaScript v1.0** — “বাংলায় কোড লেখো, JavaScript চলে!”

---
